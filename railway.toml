[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python src/simulation/simulator.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Memory optimization
memoryGB = 1
numReplicas = 1
