[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Use Dockerfile CMD for Streamlit
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Memory optimization
memoryGB = 2
numReplicas = 1
